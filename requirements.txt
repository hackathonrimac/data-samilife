# --- Peticiones HTTP y Gestión de Red ---
requests>=2.32.0           # El estándar para pedir HTML a webs estáticas
httpx>=0.27.0              # Alternativa moderna y rápida (soporta async)
fake-useragent>=1.5.0      # Para generar 'User-Agents' aleatorios y evitar bloqueos básicos

# --- Extracción y Análisis (Parsing) ---
beautifulsoup4>=4.12.0     # Para navegar y buscar elementos dentro del HTML
lxml>=5.2.0                # El motor más rápido para procesar HTML/XML con BeautifulSoup

# --- Navegación Automatizada (Para webs con JavaScript/Login) ---
selenium>=4.21.0           # Simula un navegador real (Chrome/Firefox)
webdriver-manager>=4.0.0   # Descarga y actualiza automáticamente los drivers para Selenium

# --- Manejo de Datos (Tu caso de uso) ---
pandas>=2.2.0              # Para organizar lo extraído en DataFrames
openpyxl>=3.1.0            # Para guardar/leer Excel (.xlsx)
python-dotenv>=1.0.0       # Para guardar contraseñas/proxies en un archivo .env seguro





# Framework Web y Servidor
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Base de Datos (ORM)
sqlalchemy>=2.0.0

# Validación de Datos
pydantic>=2.5.0

# Gestión de Variables de Entorno (.env)
python-dotenv>=1.0.0